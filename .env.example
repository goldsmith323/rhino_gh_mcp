# Rhino/Grasshopper MCP Server Configuration

# =============================================================================
# DEBUGGING MODE
# =============================================================================
# Controls the verbosity of tool responses
# When enabled: Returns detailed debug_log, traceback, step-by-step execution info
# When disabled: Returns minimal info (success, message, essential data only)
#
# Set to "true" for development/troubleshooting
# Set to "false" for production use (saves tokens, cleaner output)
#
# Default: false
DEBUG_MODE=false

# =============================================================================
# USAGE
# =============================================================================
# 1. Copy this file to .env (in the same directory)
# 2. Edit .env with your preferences
# 3. Restart the Rhino bridge server to apply changes
#
# Example .env file:
# DEBUG_MODE=true
